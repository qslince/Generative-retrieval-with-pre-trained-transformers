# План до 19 мая 2026

## 1-3 мая: сборка чистой структуры

- Зафиксировать основную директорию `plum_sid_coursework`.
- Перенести общий код SID в `src/sid_utils.py`.
- Разложить ноутбуки по этапам: baseline, SID generation, RecSys, analysis.
- Создать master-таблицу экспериментов.

## 4-7 мая: SID-метрики

Для каждого способа генерации SID посчитать:

- `entropy_l*`, `entropy_mean`, `entropy_min`;
- `unique_l*`, `unique_total_base`;
- `n_collisions`, `max_dupe`, `cfr`;
- `pas_emb`, `pas_behavioral`;
- `zipf_alpha_full`;
- долю test targets, попадающих в collision cluster.

## 8-12 мая: RecSys-эксперименты

Обучить одинаковую GPT2Rec-модель для вариантов:

- `improved_4code_count_suffix`;
- `improved_4code_no_suffix_popular`;
- `improved_4code_popularity_suffix`;
- `improved_4code_centroid_suffix`;
- `improved_4code_semantic_suffix`.

Минимум: один seed. Желательно: 2-3 seed для лучшего и baseline-варианта.

## 13-14 мая: анализ связи метрик

- Spearman: SID-метрика против `Recall@10`, `Recall@20`, `NDCG@10`, `NDCG@20`.
- Correlation matrix между SID-метриками.
- Отдельно общий анализ и анализ внутри controlled ablation на одном RQ-VAE.

## 15-16 мая: черновик текста

Структура текста:

- введение;
- постановка цели;
- описание PLUM/RQ-VAE/GPT2Rec;
- способы SID;
- SID-метрики;
- эксперименты;
- результаты;
- связь SID-метрик с RecSys;
- выводы.

## 17 мая: графики и таблицы

Нужны финальные артефакты:

- таблица всех экспериментов;
- bar chart `Recall@10` и `NDCG@10`;
- heatmap Spearman;
- scatter `cfr` vs `Recall@10`;
- scatter `entropy_mean` vs `NDCG@10`.

## 18 мая: полировка

- Проверить совпадение чисел в тексте и CSV.
- Убрать лишние выводы из ноутбуков.
- Подписать графики.
- Подготовить короткую речь для защиты.

## 19 мая: сдача

Сдать:

- текст курсовой;
- таблицу экспериментов;
- воспроизводимые ноутбуки/скрипты;
- ключевые графики;
- краткое резюме выводов.
